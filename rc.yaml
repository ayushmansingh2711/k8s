apiVersion: v1
kind : ReplicationController
metadata:
  name: myrc100
spec:
 replicas: 5 # number of pods
 template: #pod yaml info
    metadata:
      labels:
       color: blue
    spec: 
        containers:
         - name: mycon2
           image: nginx
           ports:
            - containerPort: 80